import { DynamicAnimateDirective } from './dynamic-animate.directive';

describe('DynamicAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
